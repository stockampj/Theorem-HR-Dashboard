import * as types from './../constants/ActionTypes';

export const changeState1 = (payload1) => ({
  type: types.CHANGE_STATE1,
  payload1
});

export const changeState2 = (payload2) => ({
  type: types.CHANGE_STATE2,
  payload2
});
